The dependancy package can also be retrieve by adding
	deb http://mirrors.credil.org/credil credil main

to your /etc/apt/sources.list.d/credil.list

To add the CREDIL archive key:
	curl http://mirrors.credil.org/credil/credil-dpkg.pgp | sudo apt-key add -



