# V2 setup defaults that differ from V1

config setup
	interfaces=%defaultroute	# in v1 boilerplate
	plutoload=%search	# in v1 boilerplate
	plutostart=%search	# in v1 boilerplate
	plutowait=no
	uniqueids=yes	# in v1 boilerplate
	# rpfilter=0

# no plutoload, plutostart, dump, plutobackgroundload, no_eroute_pass
