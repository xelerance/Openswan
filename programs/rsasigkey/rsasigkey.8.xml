<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.1.2//EN"
                   "http://www.oasis-open.org/docbook/xml/4.1.2/docbookx.dtd">
<!-- lifted from troff+man by doclifter -->
<refentry>
<refmeta>
<refentrytitle>IPSEC_RSASIGKEY</refentrytitle>
<manvolnum>8</manvolnum>
<refmiscinfo class='date'>28 Oct 2006</refmiscinfo>
</refmeta>
<refnamediv id='name'>
<refname>ipsec rsasigkey</refname>
<refpurpose>generate RSA signature key</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv id='synopsis'>
<cmdsynopsis>
  <command>ipsec</command>    
    <arg choice='plain'><replaceable>rsasigkey</replaceable></arg>
    <arg choice='opt'>--verbose </arg>
    <arg choice='opt'>--random <replaceable>filename</replaceable></arg>
    <arg choice='opt'>--rounds <replaceable>nr</replaceable></arg>
    <arg choice='opt'>--configdir <replaceable>nssdbdir</replaceable></arg>
    <arg choice='opt'>--password <replaceable>nsspassword</replaceable></arg>
    <arg choice='opt'>--hostname <replaceable>hostname</replaceable></arg>
    <arg choice='opt'>--noopt </arg>
    <arg choice='plain'>nbits </arg>
</cmdsynopsis>
<cmdsynopsis>
  <command>ipsec</command>    
    <arg choice='plain'><replaceable>rsasigkey</replaceable></arg>
    <arg choice='opt'>--verbose </arg>
    <arg choice='opt'>--configdir <replaceable>nssdbdir</replaceable></arg>
    <arg choice='opt'>--password <replaceable>nsspassword</replaceable></arg>
    <arg choice='opt'>--hostname <replaceable>hostname</replaceable></arg>
    <arg choice='opt'>--noopt </arg>
    <arg choice='opt'>--oldkey <replaceable>filename</replaceable></arg>
</cmdsynopsis>
</refsynopsisdiv>


<refsect1 id='description'><title>DESCRIPTION</title>
<para><emphasis remap='I'>Rsasigkey</emphasis>
generates an RSA public/private key pair, suitable for digital signatures, of (exactly) 
<emphasis remap='I'>nbits</emphasis> bits (that is, two primes each of exactly
<emphasis remap='I'>nbits</emphasis>/2 bits, and related numbers) and emits it on standard
 output as ASCII (mostly hex) data. <emphasis remap='I'>nbits</emphasis> must be a multiple of 16.
</para>

<para>The public exponent is forced to the value <emphasis remap='B'>3</emphasis>, which has important
speed advantages for signature checking. Beware that the resulting keys have known weaknesses as encryption keys
<emphasis remap='B'>and should not be used for that purpose</emphasis>.</para>

<para>The <option>--verbose</option> option makes<emphasis remap='I'>rsasigkey</emphasis>
give a running commentary on standard error. By default, it works in silence until it is
ready to generate output.</para>
<para>The <option>--random</option> option specifies a source for random bits. The default is
<filename>/dev/random</filename> (see
<citerefentry><refentrytitle>random</refentrytitle><manvolnum>4</manvolnum></citerefentry>).
Normally, <emphasis remap='I'>rsasigkey</emphasis> reads exactly <emphasis remap='I'>nbits</emphasis>
random bits from the source; in extremely-rare circumstances it may need more. Under Linux with
hardware random support, the special device <filename>/dev/hw_random</filename> is created. However,
the driver does not guarantee FIPS compliant random, and some hardware is so broken that it return
extremely non-random data. Therefor <filename>/dev/hw_random</filename> should <emphasis remap='B'>never</emphasis>
be used with the <option>--random</option> option. Instead, one should run the 
<citerefentry><refentrytitle>rngd</refentrytitle><manvolnum>8</manvolnum></citerefentry> daemon to
funnel randomness from <filename>/dev/hw_random</filename> into <filename>/dev/random</filename>.
</para>

<para>The <option>--rounds</option> option specifies the number of rounds to
be done by the pz_probab_prime_p probabilistic primality checker. The default,
30, is fairly rigorous and should not normally have to be overridden.</para>

<para>The <option>--Configdir</option> option specifies the nss configuration directory to use.
This is the directory where the NSS certificate, key and security modules databases reside.</para>

<para>The <option>--password</option> option specifies the nss cryptographic module authentication
password if the NSS module has been configured to require it.  A password is required by hardware
tokens and also by the internal softotken module when configured to run in FIPS mode.</para>

<para>The <option>--hostname</option> option specifies what host name to use in the first line
of the output (see below); the default is what
<citerefentry><refentrytitle>gethostname</refentrytitle><manvolnum>2</manvolnum></citerefentry>
returns.</para>

<para>The <option>--hostname</option> option suppresses an optimization of the
private key (to be precise, setting of the decryption exponent to
<emphasis remap='B'>lcm(p-1,q-1)</emphasis> rather than
<emphasis remap='B'>(p-1)*(q-1))</emphasis> which speeds up operations on it
slightly but can cause it to flunk a validity check in old RSA implementations
(notably, obsolete versions of 
<citerefentry><refentrytitle>ipsec_pluto</refentrytitle><manvolnum>8</manvolnum></citerefentry>
</para>

<para><option>--oldkey</option> option specifies that rather than generate a
new key, <emphasis remap='I'>rsasigkey</emphasis> should read an old key from
the <filename>file</filename> (the name '-' means 'standard input') and use
that to generate its output. Input lines which do not look like
<emphasis remap='I'>rsasigkey</emphasis> output are silently ignored. This
permits updating old keys to the current format.</para>

<para> The output format looks like this (with long numbers trimmed down for clarity):
<literallayout remap='.nf'>

	# RSA 2048 bits   xy.example.com   Sat Apr 15 13:53:22 2000
	# for signatures only, UNSAFE FOR ENCRYPTION
	#pubkey=0sAQOF8tZ2NZt...Y1P+buFuFn/
	Modulus: 0xcc2a86fcf440...cf1011abb82d1
	PublicExponent: 0x03
	# everything after this point is secret
	PrivateExponent: 0x881c59fdf8...ab05c8c77d23
	Prime1: 0xf49fd1f779...46504c7bf3
	Prime2: 0xd5a9108453...321d43cb2b
	Exponent1: 0xa31536a4fb...536d98adda7f7
	Exponent2: 0x8e70b5ad8d...9142168d7dcc7
	Coefficient: 0xafb761d001...0c13e98d98

</literallayout> <!-- .fi -->

The first (comment) line, indicating the nature and date of the key, and
giving a host name, is used by
<citerefentry><refentrytitle>ipsec_showhostkey</refentrytitle><manvolnum>8</manvolnum></citerefentry>
when generating some forms of key output.</para>

<para>The commented-out <option>pubkey=</option> line contains the public key,
the public exponent and the modulus combined in approximately RFC 2537 format
(the one deviation is that the combined value is given with a
<emphasis remap='I'>0s</emphasis> prefix, rather than in unadorned base-64),
suitable for use in the <filename>ipsec.conf</filename> file.</para>

<para>The <option>Modulus</option>, <option>PublicExponent</option> and <option>PrivateExponent</option>
lines give the basic signing and verification data.</para>

<para>The <option>Prime1</option> and <option>Prime2</option> lines give the
primes themselves (aka <emphasis remap='I'>p</emphasis> and <emphasis remap='I'>q</emphasis>),
largest first. The <option>Exponent1</option> and <option>Exponent2</option>
lines give the private exponent mod <emphasis remap='I'>p-1</emphasis> and
<emphasis remap='I'>q-1</emphasis> respectively. The <option>Coefficient</option>
line gives the Chinese Remainder Theorem coefficient, which is the inverse of
<emphasis remap='I'>q</emphasis>, mod <emphasis remap='I'>p</emphasis>. These
additional numbers (which must all be kept as secret as the private exponent)
are precomputed aids to rapid signature generation.</para>

<para>No attempt is made to break long lines.</para>

<para> The US patent on the RSA algorithm expired 20 Sept 2000.</para>

</refsect1>

<refsect1 id='examples'><title>EXAMPLES</title>
<variablelist remap='TP'>
 <varlistentry>
  <term><emphasis remap='B'>ipsec rsasigkey --verbose 2192 >mykey.txt</emphasis></term>
  <listitem>
   <para>generates a 2192-bit signature key  and  puts  it  in  the  file <filename>mykey.txt</filename>,
    with running commentary on standard error.  The file contents can be inserted verbatim into  a  suitable  entry  in  the
    <filename>ipsec.secrets</filename> file (see
    <citerefentry><refentrytitle>ipsec_secrets</refentrytitle><manvolnum>5</manvolnum></citerefentry>),
    and the public key can then be extracted and edited into the <filename>ipsec.conf</filename> (see
    <citerefentry><refentrytitle>ipsec_showhostkey</refentrytitle><manvolnum>8</manvolnum></citerefentry>).
   </para>
  </listitem>
 </varlistentry>
 <varlistentry>
  <term><emphasis remap='B'>ipsec rsasigkey --verbose --oldkey oldie >latest.txt</emphasis></term>
  <listitem>
   <para>takes  the  old signature key from file <filename>oldie</filename> and puts a version in the
   current format into the file <filename>latest</filename>, with running commentary on standard error.
   </para>
  </listitem>
 </varlistentry>
</variablelist>
</refsect1>

<refsect1 id='files'><title>FILES</title>
<para>/dev/random, /dev/urandom</para>
</refsect1>

<refsect1 id='see_also'><title>SEE ALSO</title>
<para>
<citerefentry><refentrytitle>random</refentrytitle><manvolnum>4</manvolnum></citerefentry>,
<citerefentry><refentrytitle>rngd</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
<citerefentry><refentrytitle>ipsec_showhostkey</refentrytitle><manvolnum>8</manvolnum></citerefentry>,
<emphasis remap='I'>Applied Cryptography, 2nd. ed., by Bruce Schneier, Wiley 1996</emphasis>,
<emphasis remap='I'>RFCs 2537, 2313</emphasis>,
<emphasis remap='I'>GNU  MP, the GNU multiple precision arithmetic library, edition 2.0.2, by Torbj Granlund</emphasis>
</para>
</refsect1>

<refsect1 id='history'><title>HISTORY</title>
<para>Written for the Linux FreeS/WAN project
&lt;<ulink url='http://www.freeswan.org'>http://www.freeswan.org</ulink>&gt;
by Henry Spencer.</para>
</refsect1>

<refsect1 id='bugs'><title>BUGS</title>
<para>There is an internal limit on
<emphasis remap='I'>nbits</emphasis>,
currently 20000.</para>
<para><emphasis remap='I'>rsasigkey</emphasis>'s run time is difficult
to predict, since <filename>/dev/random</filename> output can
be arbitrarily delayed if the system's entropy pool is low on
randomness, and  the time taken by the search for primes is also somewhat
unpredictable.  A reasonably typical time for a 1024-bit key on a quiet
100MHz  Pentium  MMX with plenty of randomness available is 20 seconds,
almost all of it in the prime searches.  Generating a 2192-bit  key  on
the  same system usually takes several minutes.  A 4096-bit key took an
hour and a half of CPU time.
</para>
<para>
The <option>--oldkey</option>  option does not check its input format as
rigorously as it might. Corrupted <emphasis remap='I'>rsasigkey</emphasis>
output may confuse it.</para>
</refsect1>
</refentry>

