RCSID $Id: Makefile.fs2_0.patch,v 1.9 2004/07/10 19:10:30 mcr Exp $
--- linux/net/Makefile.preipsec	Mon Jul 13 16:47:40 1998
+++ linux/net/Makefile	Thu Sep 16 11:26:31 1999
@@ -64,6 +64,16 @@
   endif
 endif
 
+ifeq ($(CONFIG_KLIPS),y)
+ALL_SUB_DIRS += ipsec
+SUB_DIRS += ipsec
+else
+  ifeq ($(CONFIG_KLIPS),m)
+  ALL_SUB_DIRS += ipsec
+  MOD_SUB_DIRS += ipsec
+  endif
+endif
+
 L_TARGET     := network.a
 L_OBJS	     := socket.o protocols.o sysctl_net.o $(join $(SUB_DIRS),$(SUB_DIRS:%=/%.o))
 ifeq ($(CONFIG_NET),y)
